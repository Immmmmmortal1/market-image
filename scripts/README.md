# Scripts

`run_prompt_pack.py` is the rendering engine and browser workflow implementation.

It supports:

- listing prompt packs
- serving the template-selection web UI
- rendering HTML previews
- saving limited per-image overrides
- exporting approved PNG files through local Chrome
